-- 비어 있던 `crawlers.default_company` 를 그 크롤러 이름으로 한 번 채운다.
--
-- 2026-08-29 부터 크롤러 등록·수정 화면이 이 칸을 필수로 받는다 — 비우고 저장할 수 없다
-- (`app/api/crawlers.py`). 그 전에는 비워도 됐고, 정규화가 비어 있으면 크롤러 이름을 대신
-- 썼다(2026-08-26 결정, `app/normalize/engine.py` 의 `read_parent_company`).
--
-- 화면이 필수로 바뀌면서 그 자리 채움을 그만둔다 — 모회사는 운영자가 적은 값이어야지
-- 시스템이 대신 짐작한 값이면 안 된다(2026-08-29 결정). 그런데 그 결정을 코드에서만 바꾸면
-- 이미 등록된 크롤러 중 비어 있던 것들의 모회사가 갑자기 NULL 이 된다 — 토스·우아한형제들이
-- 정확히 그 자리였다. 그래서 코드가 더 이상 짐작하지 않는 대신, **지금까지 짐작해 왔던 값을
-- 이 마이그레이션이 실제 값으로 한 번 적어 둔다.** 그 뒤로는 코드도 데이터도 "크롤러 이름이
-- 모회사다" 를 짐작이 아니라 운영자가 설정한 사실로 갖는다.
--
-- 되돌리기: 이 마이그레이션이 채운 행과, 원래부터 `default_company` 가 크롤러 이름과 같았던
-- 행을 구분할 방법이 없다. 그래서 `down` 은 아무것도 하지 않는다 — 값을 지우면 후자의 행은
-- 운영자가 실제로 그렇게 입력한 값을 잃는다. 값을 비우는 것보다 그대로 두는 편이 안전하다.

-- migrate:up
UPDATE crawlers
   SET default_company = name
 WHERE default_company IS NULL OR trim(default_company) = '';

-- migrate:down
-- 의도적으로 비워 둔다. 위 되돌리기 설명을 본다.
SELECT 1;
