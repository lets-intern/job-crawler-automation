-- 판정 칸의 근거 문장을 같이 남긴다.
--
-- 0014 를 쓸 때 분류는 한 가지였다 — 본문 글자를 그대로 옮기는 것. 그래서 값이 본문에 있는지
-- 보면 검사가 끝났다.
--
-- 그런데 직군·고용형태·경력 구분 셋은 그렇게 뽑을 수 없다. `백엔드 개발자 채용` 어디에도
-- "직군: 개발" 이라고 적혀 있지 않다. 글자 일치를 요구하면 이 셋은 영원히 빈다 — 매핑
-- 방식에서 직군 53%, 고용형태 26%, 경력 45% 였던 이유가 그것이다 (2026-08-26 사용자 지시).
--
-- 그래서 이 셋은 **닫힌 목록에서 고르고, 왜 그렇게 골랐는지 근거 문장을 함께 낸다.**
-- 근거 문장이 본문에 없으면 읽고 고른 것이 아니라 지어낸 것이라 판정을 버린다
-- (`app/classify/grounding.py`).
--
-- **그 근거 문장을 남길 자리가 이 컬럼이다.** 남기지 않으면 나중에 "이 공고가 왜 경력으로
-- 분류됐나" 에 답할 수 없고, 판정이 틀렸을 때 프롬프트를 고쳐야 할지 목록을 고쳐야 할지
-- 가릴 수 없다.
--
-- 컬럼을 셋으로 나누지 않고 JSON 하나로 둔다. 이 값은 사람이 읽는 근거이지 조회 조건이 아니고,
-- 판정 칸이 늘거나 줄 때마다 컬럼을 늘리고 싶지 않다. 모양은
-- `{"job_category": "본문 문장", ...}` 이고 살아남은 판정 칸만 들어간다.
--
-- 되돌리기: `migrate down` 이 컬럼 하나를 지운다. 사라지는 것은 근거 문장뿐이고 분류 결과와
-- `raw_jobs`, `normalized_jobs` 는 그대로다. 되돌린 뒤 근거가 다시 필요하면 그 공고를 다시
-- 분류해야 한다 — 공고당 모델 호출 하나다.

-- migrate:up
ALTER TABLE job_classifications ADD COLUMN evidence_json TEXT NOT NULL DEFAULT '{}';

-- migrate:down
ALTER TABLE job_classifications DROP COLUMN evidence_json;
