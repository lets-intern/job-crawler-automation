# Tasks: job-taxonomy - Push 3

> PRD: `.claude/tasks/todo/prd-job-taxonomy.md`
> Push 범위: 프롬프트가 트리 전체를 한 번에 보내고, 근거 검사와 "대분류만 정해짐"을 다룬다
> 상태: 진행 중

## 관련 파일

- `app/classify/classifier.py` - `build_prompt`, `classify_body`. 판정 칸 목록을 프롬프트에
  적는 지금 방식(`JUDGE_CHOICES`)의 본보기
- `app/classify/grounding.py` - `JUDGE_FIELDS` 근거 검사(근거 문장이 원문에 없으면 버림)
- `tests/fixtures/` - SK·롯데그룹·두산·네이버·토스·카카오·우아한형제들 상세 픽스처와
  `seeds/site-configs-20260826.json`의 셀렉터
- `.claude/rules/llm.md` - 근거 없는 값은 빈 칸이다

## 선행 조건

- Push 2 완료 (동적 스키마와 저장 칸이 있어야 프롬프트에 넣고 결과를 받을 수 있다)

## 작업

- [ ] 3.0 프롬프트·근거·대분류만 정해진 경우
    - [ ] 3.1 `build_prompt`가 켜진 대분류·소분류 전체 트리를 한 번에 프롬프트에 적는다.
          두 단계로 나눠 부르지 않는다(PRD 2절 "한 번에 부른다"). 트리는 `대분류: 소분류1,
          소분류2, ...` 형태로 적어 모델이 어느 소분류가 어느 대분류 아래인지 알 수 있게 한다
        - [ ] 3.1.V 검증(정규화): `FakeClient` 픽스처로 대분류·소분류를 각각 하나씩 고른
              응답을 주고 `classify_body`가 `job_major`/`job_minor`를 채우는지 pytest
    - [ ] 3.2 근거 검사. `job_major_evidence`, `job_minor_evidence`가 원문에 없으면 그
          판정을 버린다. 판정 칸과 같은 처리 경로(`app/classify/grounding.py`)를 그대로 탄다
        - [ ] 3.2.V 검증(정규화): 근거 문장이 원문에 없는 응답이 버려지고 그 사실
              (`dropped`/`reasons`)이 남는지 pytest
    - [ ] 3.3 소분류만 못 고른 경우. `job_major`는 정했는데 `job_minor`가 `판단불가`거나
          근거가 없으면 대분류만 남기고 소분류는 비운다. 대분류 자체가 `판단불가`면 둘 다
          비운다(대분류 없이 소분류만 있는 상태를 만들지 않는다)
        - [ ] 3.3.V 검증(정규화): 대분류만 있는 응답, 근거 검사 실패 응답, `대분류
              판단불가` 응답 세 가지를 각각 픽스처로 돌려 저장되는 값이 규칙대로인지 pytest
    - [ ] 3.4 실측 1회. 상세가 HTML인 일곱 곳 픽스처를 실제 제공자로 돌려, 씨앗 296개짜리
          목록을 프롬프트에 다 넣었을 때 모델이 실제로 표 안의 이름을 고르는지, 대분류·
          소분류가 서로 맞는 조합으로 나오는지 본다. 채운 건수와 버린 건수를 결과보고서에
          적는다("모델이 296개 중에서 실제로 고르는가" — PRD "열려 있는 것")
        - [ ] 3.4.V 검증(셀렉터 생성): 일곱 곳 각각 1회 실제 호출, 나온 `job_major`/
              `job_minor`가 표에 있는 이름인지, 소분류가 그 대분류 소속이 맞는지 대조한 표를
              결과보고서에 남긴다
