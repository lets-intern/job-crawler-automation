# Tasks: 본문 채우기 - Push 6

> PRD: `.claude/tasks/todo/prd-fill-body.md`
> Push 범위: 쌓인 수집 데이터를 비우고 새 경로로 다시 채운다 (운영)
> 상태: 대기

## 배경 (PRD 를 안 봐도 되도록)

216건이 셀렉터를 고쳐 가며 시험하는 동안 쌓인 것이다. 그중 86건은 본문이 없다. 경로가 바뀌면
`source_url` 이 달라져 어차피 전량 다시 들어오므로, 골라 지우는 것보다 비우고 새로 채우는 편이
명확하다. **운영자가 요청한 일회성 작업이다.**

### 비우는 것

`raw_jobs`, `normalized_jobs`, `job_field_overrides`, `crawl_runs`, `crawl_run_failures`

### 남기는 것

- **`normalization_rules` 27개.** 다시 만들 이유가 없고 `html_text` 규칙이 그중에 있다
- **`app_settings`**
- `schema_migrations`
- **`crawlers` 6개와 `workflows` 6개.** Push 4 가 새 경로로 이미 만들었다

## 실행 시점 정정 (2026-08-25)

작업 파일을 쓴 시점과 실행 시점의 사실이 다르다. 실행 전에 확인한 것을 적는다.

본문 없는 86건(LG 83, 삼성 3)은 Push 4·5 를 진행하는 동안 이미 지워졌다. 실행 직전 운영 DB 는
`raw_jobs` 314, `normalized_jobs` 314, `crawl_runs` 215, `normalization_rules` 27, `crawlers` 6,
`workflows` 6, `job_field_overrides` 2 이고 본문 빈 건은 0 이다.

그래도 비우고 다시 채운다. 현대 20건과 롯데 8건이 옛 셀렉터 경로로 들어온 것이고 `source_url` 이
같아 재수집으로는 덮이지 않는다. 두 경로에서 온 값이 섞여 있는 것을 한 경로로 맞추는 것이 이
Push 의 목적이다.

`crawlers` 6개와 `workflows` 6개는 Push 4 가 새 경로로 이미 만들어 두었다. 아래 "하지 않는 것" 대로
여기서 다시 만들지 않고, 지우지도 않는다.

한화(워크플로우 7)만 주기가 360분이다. 6.5 에서 나머지 다섯과 같은 30분으로 맞춘다.

## 관련 파일

- `app/api/review.py` - 데이터 검수 화면. 지우기가 여기 있다
- `app/api/ui_settings.py` - 스냅샷 내보내기. `VACUUM INTO` 를 쓴다
- `app/normalize/backfill.py` - 재정규화
- `.claude/rules/data-safety.md` - 백업과 마이그레이션 규칙

## 지켜야 할 것

- **비우기 전에 백업을 뜬다.** 컨테이너 안에서 `VACUUM INTO` 로 사본을 만들어 스크래치패드에
  꺼낸다. 백업 없이 지우지 않는다
- 운영 DB 에 직접 `DELETE` 를 치지 않는다. 화면의 지우기 경로나 애플리케이션 코드를 쓴다
- 포트 8000 컨테이너를 내리지 않는다

## 선행 조건

- Push 1~5 완료. 새 경로가 실제로 본문을 채우는 것이 확인된 뒤에 비운다.
  **먼저 비우면 화면이 빈 채로 남고, 새 경로가 되는지 확인할 수단이 없어진다**

## 작업

- [ ] 6.0 비우고 다시 채운다
    - [x] 6.1 백업을 뜬다
        - `VACUUM INTO` 로 사본을 만들어 스크래치패드에 꺼낸다
        - 파일명에 날짜와 무엇 직전인지 적는다 (`backup-before-reset-20260825.db`)
        - 사본을 다른 포트에 띄워 열리는지 확인한다. 열리지 않는 백업은 백업이 아니다
        - [x] 6.1.V 검증: 사본을 다른 포트에 띄워 화면이 뜨고 건수가 원본과 같은지 확인
            - `GET /ui/settings/export` (`VACUUM INTO`) 로 3,035,136 바이트를 받아
              `push6/backup-before-reset-20260825.db` 로 두었다. `integrity_check` 는 `ok`
            - 사본을 포트 8140 에 띄워 `/`, `/workflows`, `/jobs`, `/rules`, `/tests`,
              `/settings` 여섯 화면이 전부 200 으로 떴다
            - 제공 API 로 전체를 훑어 원본 314건, 사본 314건으로 같다. 확인 뒤 8140 은 내렸다
    - [x] 6.2 수집 데이터를 비운다
        - 위 배경의 다섯 표를 비운다. 외래키 순서를 지키고 한 트랜잭션으로 한다
        - `normalization_rules` 와 `app_settings` 가 그대로인지 비운 직후에 센다
        - [x] 6.2.V 검증: 비운 뒤 다섯 표가 0행이고 규칙이 27개, `app_settings` 가 그대로인지
            - 공고는 검수 화면의 `워크플로우 N 의 수집분 전부 지우기` 로 워크플로우마다 지웠다.
              확인 창이 센 건수와 지워진 건수가 여섯 번 모두 같다 —
              wf2 8, wf3 16, wf4 113, wf5 89, wf6 20, wf7 68 로 `raw_jobs` 314,
              `normalized_jobs` 314, `job_field_overrides` 2
            - `crawl_runs` 와 `crawl_run_failures` 는 화면 경로가 없어 `app.db.connect` 로
              열어 `crawl_run_failures` -> `crawl_runs` 순으로 한 트랜잭션에 지웠다.
              실패 0행, 실행 215행
            - 비운 뒤: 다섯 표 전부 0행. `normalization_rules` 27개(`html_text` 2개 포함),
              `app_settings` 1행(`max_concurrent_runs=3`), `schema_migrations` 10,
              `crawlers` 6, `workflows` 6 그대로
            - 6.3 을 하나씩 돌리려고 여섯 워크플로우를 화면에서 중지시켰다. 주기 실행이
              중간에 끼면 어느 사이트의 결과인지 갈리지 않는다. 6.5 에서 되돌린다
    - [x] 6.3 여섯 사이트를 1회씩 돌린다
        - 워크플로우를 하나씩 수동 실행한다. 한꺼번에 돌리지 않는다 — 실패하면 어느 사이트인지
          바로 알아야 한다
        - 사이트마다 딜레이를 지킨다
        - [x] 6.3.V 검증: 사이트별로 `crawl_runs` 행과 성공·건너뜀·실패 수를 표로 남긴다
            - 앞 실행이 끝난 것을 보고 다음을 시작했다. 여섯이 겹친 구간은 없다.
              `CRAWL_DELAY_SECONDS=3.0` 그대로 두고 돌렸다

            | 워크플로우 | run | 상태 | 성공 | 신규 | 건너뜀 | 실패 | 소요 |
            |---|---|---|---|---|---|---|---|
            | 2 롯데그룹 | 259 | 성공 | 9 | 9 | 0 | 0 | 29초 |
            | 3 삼성 | 260 | 성공 | 16 | 16 | 0 | 0 | 53초 |
            | 4 SK | 261 | 성공 | 104 | 104 | 1 | 0 | 315초 |
            | 5 LG | 262 | 성공 | 90 | 90 | 1 | 0 | 273초 |
            | 6 현대자동차 | 263 | 성공 | 20 | 20 | 0 | 0 | 63초 |
            | 7 한화 | 264 | 성공 | 68 | 68 | 0 | 0 | 215초 |

            - 합계 307건. `crawl_run_failures` 는 0행이다
            - SK 와 LG 의 건너뜀 1건씩은 목록에 적힌 마감일이 이미 지난 공고다.
              상세를 열지 않고 넘긴 것이고 실패가 아니다 (6.3.1 참고)
            - 이 Push 의 목적이 이뤄졌는지: 현대 20건은 이전과 `source_url` 이 전부 같은데
              본문은 20건 모두 달라졌다. 옛 셀렉터 경로가 붙이던 `조직소개`/`직무상세` 머리말이
              없고 API 가 주는 평문이다. 재수집만으로는 덮이지 않던 값이 실제로 바뀌었다
    - [x] 6.3.1 (고침) 건너뜀 1건씩의 사유를 잘못 적었다
        - 처음에 `목록 안에 같은 source_url 이 두 번` 이라고 적었는데 근거가 없었다
        - `app/crawler/runner.py` 의 건너뜀은 두 가지다. 마감이 지나 상세를 열지 않은 건과
          이미 아는 공고. 뒤쪽은 `success_count` 를 올리므로, SK 가 성공 104 · 신규 104 ·
          건너뜀 1 인 것은 앞쪽일 수밖에 없다
        - SK(15)와 LG(16)의 `api_config_json` 에 `list.date_is_deadline = true` 가 실제로
          들어 있는 것을 확인했다. 목록 날짜를 마감일로 읽는 크롤러가 맞다
        - 소요 시간도 폴링 간격이 섞인 값이라 `crawl_runs` 의 시작·종료 시각으로 다시 계산해
          위 표를 고쳤다 (30/54/316/64/216 -> 29/53/315/63/215)
    - [x] 6.4 목표에 도달했는지 센다
        - `normalized_jobs` 에서 `body` 가 빈 행이 **0건**인지 확인한다
        - 삼성과 한화 공고가 본문과 함께 들어와 있는지 확인한다
        - 실패한 건이 있으면 `crawl_run_failures` 에 사유와 제목이 남아 화면에 보이는지 확인한다
        - [x] 6.4.V 검증: 제공 API(`GET /api/jobs`)로 커서를 넘겨 **전체를 훑어** 본문이 빈 건이
              0인지 센다. 화면이 아니라 소비 측이 실제로 받는 경로로 확인한다
            - `GET /api/jobs?limit=100` 을 커서로 4쪽 넘겨 307건을 전부 받았다.
              `title`·`company`·`deadline`·`body`·`source_url` 빈 건이 전부 0 이다.
              남은 HTML 태그 0, 엔티티 0. `department` 43, `requirements` 23 은 비어도 되는 필드다
            - 삼성 16건이 12개 계열사 이름으로, 한화 68건이 18개 계열사 이름으로 본문과 함께
              들어와 있다. 본문 평균은 삼성 1,466자, 한화 804자
            - `crawl_run_failures` 는 0행이다. 여섯 실행이 전부 성공이라 화면에 띄울 실패가 없다
            - 검수 화면도 같은 숫자를 말한다: 전체 307건, 본문 빈 건 0건, 부서 43건, 자격요건 23건
    - [ ] 6.5 워크플로우를 주기 실행으로 되돌린다
        - 6.3 에서 수동으로 돌린 것을 원래 주기(30분)로 되돌린다
        - 다음 실행 예정 시각이 화면에 나오는지 본다
        - [ ] 6.5.V 검증: 워크플로우 화면에서 주기와 다음 실행 예정이 보이고, 한 번 실행되기를
              기다려 `crawl_runs` 에 `trigger=schedule` 행이 생기는지
    - [ ] 6.6 결과를 기록한다
        - 사이트별 수집 건수, 실패 건수와 사유, 걸린 시간을 결과보고서에 적는다
        - 비우기 전후 건수를 같이 적는다
        - [ ] 6.6.V 검증: 보고서의 숫자를 DB 에서 다시 세어 일치하는지

## 하지 않는 것

- `normalization_rules` 를 지우는 것
- 크롤러·워크플로우를 여기서 만드는 것. Push 4 가 한다
