# Tasks: job-taxonomy - Push 4

> PRD: `.claude/tasks/todo/prd-job-taxonomy.md`
> Push 범위: 직무 분류 어드민 화면. 목록·더하기·고치기·켜기끄기·씨앗 넣기·공고 수
> 상태: 진행 중

## 관련 파일

- `app/api/ui.py` - `NAV_GROUPS`. 어느 묶음에 넣을지는 `정규화` 묶음(`/rules`, `/side`)이
  적합하다 — 분류 체계는 정규화 파이프라인의 입력이지 수집이 아니다
- `app/api/ui_companies.py`, `app/templates/pages/companies.html`,
  `app/templates/fragments/company_list.html`/`company_row.html` - 목록+행별 켜기끄기+
  고치기 폼이 한 화면에 있는 CRUD 화면의 본보기
- `app/taxonomy.py`(Push 1) - 읽기·쓰기·씨앗 넣기

## 선행 조건

- Push 1 완료 (표와 저장소만 있으면 된다. Push 2·3과 무관하게 이 화면을 만들 수 있다)

## 작업

- [ ] 4.0 어드민 화면
    - [x] 4.1 네비게이션에 `직무 분류`를 더한다(`/taxonomy`, `정규화` 묶음)
        - [x] 4.1.V 검증(화면): 화면을 열고 네비게이션이 켜지는지 확인
        - [x] 4.1.1 (수정) `tests/test_ui_nav_groups.py` 가 묶음 화면 수를 8로 가정해 깨졌다.
              9로 고치고 `/taxonomy` 를 파라미터 목록에 더한다
    - [ ] 4.2 목록 화면. 대분류와 그 아래 소분류를 들여쓰기나 그룹으로 보여주고, 각 행에
          켜짐·꺼짐 상태와 **그 이름으로 이미 분류된 공고 수**(`normalized_jobs.job_major`/
          `job_minor`를 센다)를 함께 적는다. 표가 비어 있으면 씨앗 넣기 버튼만 보인다
        - [ ] 4.2.V 검증(화면): 로컬에서 화면을 열고 대분류·소분류가 트리로 보이는지,
              공고 수가 실제 값과 맞는지 확인
    - [ ] 4.3 더하기·고치기 폼. 이름, 부모(대분류 선택 또는 없음=대분류로 등록), 순서,
          메모를 받는다. 이름을 고칠 때 **그 이름으로 이미 분류된 공고가 몇 건인지**를
          저장 전에 화면에 보여준다(PRD 3절 "고치면 그 건들의 값과 목록이 어긋난다")
        - [ ] 4.3.V 검증(화면): 화면에서 대분류·소분류를 만들고 고쳐서 목록에 반영되는지,
              이름을 고칠 때 건수 경고가 뜨는지 확인
    - [ ] 4.4 켜기·끄기 단추. 끄면 목록에서 "꺼짐"으로 표시되고 그 값으로 이미 분류된
          공고 수는 그대로 보여준다(지운 것이 아니므로)
        - [ ] 4.4.V 검증(화면): 켜고 끄면 상태와 문구가 바뀌는지, 지우기 단추 자체가 화면에
              없는지 확인
    - [ ] 4.5 씨앗 넣기 버튼. 표가 완전히 비어 있을 때만 보인다. 누르면
          `seeds/job-taxonomy-zighang-20260828.json`을 한 번에 넣고, 넣고 나면 이 버튼은
          사라진다(이미 채워진 표 위에 다시 누를 방법을 화면에 남기지 않는다)
        - [ ] 4.5.V 검증(화면): 빈 표에서 씨앗 넣기를 누르면 대분류 25개·소분류 296개가
              들어오고, 그 뒤에는 버튼이 사라지는지 확인
