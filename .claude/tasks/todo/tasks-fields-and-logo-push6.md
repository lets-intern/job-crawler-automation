# Tasks: fields-and-logo - Push 6

> PRD: `.claude/tasks/todo/prd-fields-and-logo.md`
> Push 범위: 회사 화면. 로고를 올리고 모회사 이름을 고친다
> 상태: 진행 중

## 관련 파일

- `app/api/ui.py` - `NAV`
- `app/templates/pages/crawlers.html`, `app/templates/fragments/crawler_list.html` - 본보기
- `app/templates/macros.html` - `empty`, `notice_box`, `wait`
- `.claude/rules/writing.md` - 상태는 낱말로

## 선행 조건

- Push 4 완료 (`companies` 표)
- Push 5 완료 (올릴 곳)

## 작업

- [ ] 6.0 회사 화면
    - [x] 6.1 `NAV` 에 `회사` 를 더하고 페이지를 만든다
        - [x] 6.1.V 검증(화면): 열어서 네비게이션이 켜지는지 확인.
              `/companies` 가 200 이고 그 항목에 `aria-current="page"` 가 붙는다.
              목록 조각(`/ui/companies`)이 저장된 회사와 모회사·로고를 그리고, 행이 없으면
              언제 생기는지 적는다. `tests/test_ui_companies.py` 6개 통과
    - [x] 6.2 목록. 회사명·모회사·로고·공고 수. **기본 정렬은 공고 많은 순**이다. 로고 하나가
          몇 건에 붙는지가 무엇을 먼저 등록할지를 정한다
        - [x] 6.2.V 검증(화면): 공고 수가 다른 회사를 섞어 놓고 순서를 확인.
              공고 3건·1건·0건인 회사 셋을 섞어 넣으니 표가 `카카오`(3건) → `가나다`(1건)
              → `공고없는회사`(0건) 순으로 나왔다. 공고가 없는 회사도 0건으로 남는다 —
              빠지면 로고를 지울 회사를 화면에서 찾을 수 없다.
              `tests/test_ui_companies.py` 9개 통과
    - [x] 6.3 조회 조건 둘. `로고 없음` 과 `공고 N건 이상`. 둘을 걸면 "로고가 없으면서 공고가
          여러 개인 회사" 만 남는다 — 이것이 등록할 목록이다. 공고 한 건짜리 회사를 먼저
          등록하느라 백 건짜리가 뒤에 서면 안 된다
        - [x] 6.3.V 검증(화면): 조건을 걸어 걸린 회사 수와 공고 합계가 실제와 맞는지 확인.
              공고 3·2·1건인 회사 셋에 `공고 2건 이상` 을 걸면 `조건에 걸린 회사 2곳,
              공고 합계 5건` 이 나온다. 둘을 함께 걸면 로고가 없으면서 공고가 여러 개인
              회사 하나만 남는다. 숫자 칸을 비우면 조건 없음으로 읽는다 — 정수로 받으면
              그것이 422 가 되어 조건을 지우려던 조작이 오류 조각으로 돌아온다.
              `tests/test_ui_companies.py` 16개 통과
    - [x] 6.4 등록한 로고는 그 회사명을 가진 공고 전부에 붙는다. 회사명으로 잇는 것이라
          공고마다 따로 넣는 동작은 만들지 않는다. 붙는 건수를 저장 뒤에 문장으로 알린다.
          저장 경로는 `PUT /ui/companies/logo` 하나이고, 주소를 비우면 지운다 —
          지우기 단추를 따로 두지 않는다. 붙여넣기 길(6.7)이 이 경로를 쓴다
        - [x] 6.4.V 검증(화면): 공고 여러 건인 회사에 로고를 넣고, 그 건수가 알림과 같은지 확인.
              실사로 확인했다. 공고 7·3·1건인 회사 셋을 넣은 DB 로 서버를 띄우고(:8010)
              화면에서 삼성SDS(7건)에 로고를 저장하니 `저장했다. 이 로고는 회사명이 같은
              공고 7건에 붙는다 — 공고마다 따로 넣지 않는다` 가 나왔고, 그 줄만 갈렸다.
              같은 화면에서 목록이 `3곳, 공고 합계 11건`, 조건을 걸면 `2곳, 공고 합계 10건`
              이었다. `tests/test_ui_companies.py` 23개 통과
    - [x] 6.5 로고 주소가 지금 `s3_public_base` 로 시작하지 않는 행에 `옛 저장소` 를 적는다.
          엔드포인트를 바꾼 뒤 무엇을 다시 올려야 하는지 눈에 보이게 한다.
          밖에 올려 둔 주소를 붙여넣은 행(6.7)도 같은 표시를 받는다 — 주소가 어디서 왔는지는
          저장하지 않아 둘을 가릴 수 없다. 마이그레이션 없이 가리는 방법이 없으므로 그
          사실을 화면 설명에 적었다
        - [x] 6.5.V 검증(화면): 설정을 바꾼 뒤 기존 행에 그 표시가 뜨는지 확인.
              실사로 확인했다. 공개 주소가 `http://localhost:9000` 인 동안 같은 주소로
              시작하는 로고에는 표시가 없었고(0건), `/ui/storage` 에서 공개 주소를
              `https://cdn.example.com/logos` 로 저장하니 그 행에
              `<span class="badge badge-warn">옛 저장소</span>` 가 붙었다.
              `tests/test_ui_companies.py` 28개 통과
    - [x] 6.6 파일을 골라 올린다. 올린 뒤 그 자리에 미리보기가 나온다.
          저장소가 채워지지 않았으면 파일 고르기를 아예 내지 않고 무엇을 채워야 하는지
          적는다 — 눌러도 아무 일이 없는 단추를 두면 로고가 왜 안 붙는지 알 수 없다.
          저장소가 거절하면 사유가 그 줄에 남는다. 객체 이름에 회사명을 넣지 않는다
        - [x] 6.6.V 검증(화면): 이미지를 올려 목록에 뜨고 새로 고쳐도 남는지 확인.
              실사로 확인했다. 도커로 떠 있는 MinIO 를 `logos` 버킷으로 설정하고
              (`/ui/storage` 연결 확인 성공) 화면에서 PNG 를 올리니
              `http://localhost:9000/logos/company/8e10f593...png` 가 되고, 그 주소가
              `200 image/png` 로 열렸다. 그 자리에 `<img>` 미리보기가 나오고 목록을 다시
              불러도 남았다. SVG 를 올리면 `올리지 못했다 / 사유: not_an_image` 다.
              픽스처 몫은 `tests/test_ui_companies.py` 34개 통과
    - [x] 6.7 이미 어딘가에 올려 둔 주소를 붙여넣는 길. 우리 저장소를 거치지 않는다.
          저장 경로는 6.4 가 만든 `PUT /ui/companies/logo` 다. 이 커밋이 더한 것은 그 길이
          받는 것을 서버에서 보는 일이다 — 브라우저의 `type="url"` 은 화면을 지나는 값만
          막고, `javascript:` 로 시작하는 값이 저장되면 그것이 곧 목록의 링크가 된다
        - [x] 6.7.V 검증(화면): 주소를 붙여넣어 저장되고 미리보기가 뜨는지 확인.
              실사로 확인했다. `https://cdn.elsewhere.test/daangn.png` 를 붙여넣으니
              저장되고(`companies.logo_url` 확인) 그 자리에 `<img>` 미리보기와 `옛 저장소`
              표시가 나왔다 — 우리 저장소 밖의 주소라 예정된 표시다.
              `javascript:alert(1)` 은 `주소를 받지 않았다 / 사유: invalid_input` 로
              거절되고 옛 주소는 그대로 남았다. `tests/test_ui_companies.py` 38개 통과
    - [ ] 6.8 모회사 이름을 고친다. 정규화가 만든 값을 사람이 덮는 자리다
        - [ ] 6.8.V 검증(화면): 고친 값이 다음 정규화에 덮이지 않는지 확인
