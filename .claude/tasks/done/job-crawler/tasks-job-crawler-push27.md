# Tasks: job-crawler - Push 27

> PRD: `.claude/tasks/done/job-crawler/prd-job-crawler.md`
> Push 범위: 데이터 조회에서 상세하게 걸러 내고, 걸린 것을 지운다
> 상태: 완료

## 배경

셀렉터를 고쳐 가며 시험하는 동안 잘못 뽑힌 공고가 쌓였다. 지울 방법이 화면에 없다.

`raw_jobs` 가 append-only 라는 규칙은 **파이프라인이 자기가 넣은 것을 고쳐 쓰지 못하게** 하는
것이다 (`.claude/rules/data-safety.md`). 운영자가 명시적으로 고른 행을 지우는 것은 그것과 다르고,
규칙이 요구하는 대로 응용 코드 안에서 일어난다.

## 두 화면의 역할

지우는 자리는 **데이터 조회**(`/jobs`)다. 상세한 조건으로 좁힌 다음 걸린 것을 지운다.
**데이터 검수**(`/review`)는 값을 고치는 자리고, 지우기를 두지 않는다.

한 화면이 고치기와 지우기를 같이 들고 있으면 체크박스 옆에서 값을 고치게 되고, 고치려다
지우는 사고가 그 자리에서 난다.

## 관련 파일

- `app/api/ui_jobs.py` - 조회 화면. 필터·검색·정렬이 이미 있다
- `app/templates/fragments/job_filters.html`, `job_table.html`
- `app/templates/pages/jobs.html`
- `migrations/0001_initial_schema.sql:62` - `normalized_jobs.raw_job_id -> raw_jobs(id)`
- `migrations/0005_job_field_overrides.sql:29` - `job_field_overrides.raw_job_id -> raw_jobs(id)`

## 선행 조건

- 없음. Push 23 과 파일이 겹치지 않는다 (그쪽은 `app/crawler/` 와 마이그레이션)

## 작업

- [x] 27.0 데이터 조회에서 걸러 내고 지운다
    - [x] 27.5 조회 필터를 상세하게 넓힌다
        - 실제 열에 있는 것만 넣는다. 없는 것을 만들지 않는다
        - 워크플로우(사이트), 회사명, 진행 여부(마감 지남 / 진행중 / 마감일 없음),
          수집 시각 범위, 정규화 시각 범위, 제목 검색, 전달 여부
        - 회사명은 한 사이트에 계열사가 여럿이라 워크플로우와 따로 걸린다
        - [x] 27.5.V 검증: 사본 DB 로 띄운 서버에서 조건별 건수가 직접 센 수와 같은지
              사본(148건)을 8075 에 띄우고 15가지 조건을 직접 센 수와 맞췄다. 전부 같다.
              `수집 2026-08-21 하루` 가 0건인 것이 시간대 처리가 맞다는 증거다 — 저장된
              `2026-08-21 19:30 UTC` 는 표시 시간대로 22일이다
    - [x] 27.6 필터에 걸린 것을 지운다
        - 대상은 **지금 필터에 걸린 것**이다. 페이지 단위가 아니다
        - 행마다 체크박스와, 이 페이지만 고르는 것과 필터 전체를 고르는 것을 나눠 둔다.
          **"이 페이지 20건" 과 "필터에 걸린 148건" 은 다르다.** 고른 수를 항상 숫자로 보인다
        - 하나도 고르지 않으면 지우기 단추는 눌리지 않는다
        - 세 표를 같이 지운다: `job_field_overrides` -> `normalized_jobs` -> `raw_jobs`.
          외래키 순서를 지킨다. 한 트랜잭션이다
        - 지우기 전에 몇 건인지 보이고 확인을 받는다. **`raw_jobs` 는 다시 만들 수 없다** —
          마감된 공고는 사이트에서 이미 내려가 다시 수집되지 않는다
        - `delivered_at` 이 찍힌 행이 섞여 있으면 그 수를 따로 알린다. 소비 측은 이미 받아 갔고
          지운다고 그쪽에서 사라지지 않는다
        - 지운 건수와 누가 요청했는지를 로그에 남긴다
        - 브라우저 `confirm()` 을 쓰지 않는다. 이 저장소가 쓰는 `<dialog>` + HTMX 방식을 따른다
        - [x] 27.6.V 검증: 사본 DB 로 띄운 서버에서 3건을 골라 지우고 세 표에서 모두 사라졌는지,
              고르지 않은 행이 남았는지 확인. 운영 DB 에 하지 않는다
              사본(148/148/2)을 8075 에 띄우고 Chromium 으로 3건(188, 187, 186)을 지웠다.
              145/145/2 가 됐고 그 세 id 는 세 표 어디에도 없다. 고르지 않은 145건은 그대로다.
              확인 창은 `표에서 고른 공고`, `되돌릴 수 없다`, 세 표의 건수를 적었다
        - [x] 27.6.1 지운 뒤 표를 다시 부를 때 조건을 잃지 않게 한다
              27.6.V 에서 브라우저 콘솔에 오류가 났다:
              `The selector "#job-filters form" on hx-include returned no matches!`
              표가 `load` 로 처음 뜰 때 조회 조건 폼은 아직 들어오지 않았는데 그것을
              `hx-include` 로 가리켰다. 표가 조건을 끌어오는 대신 조건 폼이 `jobs-deleted` 를
              듣고 스스로 다시 조회하게 바꿨다. 27.6 이 아직 커밋되지 않은 상태에서 찾은 것이라
              같은 커밋에 들어간다 — 오류를 한 번 커밋했다가 지우는 자국을 남기지 않는다
    - [x] 27.7 워크플로우 단위로 전부 지우는 길을 둔다
        - 한 사이트의 수집분을 통째로 비우는 경우가 잦다. 148건을 20건씩 페이지로 고르게 하지 않는다
        - 워크플로우를 지우는 것이 아니라 그 워크플로우가 모은 공고만 지운다
        - 정규화되지 않은 수집 건까지 포함한다. 필터는 정규화된 것만 보여주기 때문이다
        - [x] 27.7.V 검증: 사본 DB 에서 워크플로우 하나의 수집분만 사라지고 다른 것은 그대로인지
              사본(148건)에서 워크플로우 6(현대자동차, 20건)을 통째로 지웠다. 148 -> 128 이고
              다른 넷은 6/3/30/89 그대로, 워크플로우 행 5개도 그대로다.
              지우기 직전 화면은 걸린 수가 0건인 회사로 좁혀져 있었는데도 20건이 지워졌다 —
              워크플로우 범위가 나머지 조건을 보지 않는다는 것이 그대로 동작했다. 확인 창이
              그 사실을 적고, 걸리지도 않는 조건은 조건 줄에서 뺀다
    - [x] 27.8 다시 수집된다는 것을 화면에 적는다
        - 아직 게시 중인 공고를 지우면 다음 실행에서 다시 들어온다. `content_hash` 를 아는
          공고인지로 중복을 가리는데, 지우면 그 기억도 같이 사라지기 때문이다
        - 지우기 확인 창에 이 문장을 넣는다. 모르고 지우면 왜 되살아나는지 알 수 없다
        - [x] 27.8.V 검증: 사본 DB 에서 진행중인 공고를 지우고 같은 워크플로우를 1회 실행해
              다시 들어오는지 확인
              실사이트에 가지 않았다. 로컬 정적 서버(8076)를 사이트로 세우고 그것을 보는
              워크플로우를 사본에 만들었다 — 확인하려는 것은 어느 사이트의 마크업이 아니라
              "지우면 아는 공고인지를 가리는 기억도 사라진다" 는 동작이고, 운영 중인 사이트에
              검증용 요청을 더 보내지 않는다 (`.claude/rules/crawling.md`).
              1회차 3건 적재 -> 2회차 0건(아는 공고) -> 화면에서 3건 삭제 -> 3회차 3건 재적재.
              `raw_jobs.id` 는 새로 붙었고 `content_hash` 세 개는 1회차와 같다
    - [x] 27.9 검수 화면은 고치는 일에만 쓴다
        - 지우기를 두지 않는다. 27.1 ~ 27.4 로 넣었던 것은 `d45b6f8` 에서 되돌렸다
        - 고치는 흐름을 막는 것만 치운다. 새 기능을 만들지 않는다
        - 보정 삭제의 브라우저 `confirm()` 을 뺐다. 여기서 사라지는 것은 사람이 친 값 하나이고
          되돌릴 규칙값이 바로 아래에 그대로 적혀 있다 — 되돌릴 수 없는 일이 아니다. 여섯
          필드를 오가며 누르는 자리라 그때마다 브라우저 창이 뜨면 고치는 일 자체가 막힌다
        - [x] 27.9.V 검증: 검수 화면에 지우기 경로가 남아 있지 않은지, 고치기가 그대로 도는지
              사본으로 도는 8075 에서 Chromium 으로 확인. 고르기 폼·행 체크박스·지우기 단추가
              모두 0개이고 `/ui/review/delete*` 는 404 다. 제목을 고쳐 저장하니 보정이 쌓이고
              모달이 닫히고 표의 그 칸이 갈렸다. 보정 삭제는 브라우저 창 없이 됐고
              (`dialog` 이벤트 0회) 모달은 열린 채다. 148/148 행은 그대로다

## 취소된 작업

방향이 바뀌어 접었다. 지우기를 검수 화면에 넣었다가 조회 화면으로 옮겼다.

- 27.1 검수 화면 행마다 체크박스와 전체 선택 (`4affb12`, `d45b6f8` 에서 되돌림) -> 27.6
- 27.2 검수 화면에서 고른 공고 지우기 -> 27.6
- 27.3 검수 화면의 워크플로우 단위 지우기 -> 27.7
- 27.4 검수 화면 확인 창의 재수집 안내 -> 27.8

## 하지 않는 것

- 마감된 공고를 자동으로 지우는 것. 지우는 것은 사람이 고른다
- `raw_jobs` 를 고쳐 쓰는 것. 지우거나 두거나 둘 중 하나다
- 검수 화면에 지우기를 두는 것
