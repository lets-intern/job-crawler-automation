# Tasks

```
todo/    PRD 및 진행 중 task 파일
done/    완료된 task
memos/   그 외 메모
```

`task-maker` 가 PRD 를 여기에 분해하고, `task-runner` 가 실행한다.
체크박스와 단어로만 상태를 표시한다. 이모지·아이콘은 쓰지 않는다 (`.claude/rules/writing.md`).
