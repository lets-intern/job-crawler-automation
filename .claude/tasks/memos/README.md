# 메모

아직 하지 않기로 한 구상과, 그 판단의 근거가 된 사용자 메모를 둔다.
여기 있는 것은 작업 대상이 아니다. 작업할 것은 `.claude/tasks/todo/` 에 있다.

| 파일 | 무엇 | 왜 보류 |
|---|---|---|
| `prd-crawler-v2-보류.md` | 본문을 13개 항목으로 나누고, AI 제공자 넷을 고르고, 호출 로그 DB 를 두는 구상 | 2026-08-25 측정에서 제목·회사·마감이 216건 전부 채워져 있고 롯데·SK·현대는 본문도 다 있는 것이 확인됐다. 지금 비어 있는 것은 LG 구 데이터와 삼성뿐이라, 나누기 전에 채우는 것이 먼저다 |
| `크롤링 ocr변경.md`, `크롤링 ocr변경2.md` | 사용자가 적은 v2 구상 원본 | 위 PRD 의 출처. 결정 과정을 남긴다 |

여기 담긴 측정값은 그대로 쓸 수 있다. 경로별 시간·토큰은
`.claude/docs/ocr-benchmark.md`, 사이트별 확인 결과는 `.claude/site-recipes/` 에 있다.
