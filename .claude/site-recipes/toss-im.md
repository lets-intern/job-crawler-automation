# 토스 (toss.im)

- 리스트 URL: https://toss.im/career/jobs
- 수집 방식: 목록은 `playwright`, 상세는 `static` (크롤러 33, 2026-08-25 등록)
- 상세 URL 형식: `https://toss.im/career/job-detail?job_id=<번호>` (목록 항목의 `href` 그대로)
- 페이지네이션: 없다. 렌더하면 한 화면에 263건이 다 들어 있다 (2026-08-25 측정)
- 날짜 포맷: 목록에 날짜가 없다. `list.date` 는 빈 채로 저장돼 있고, 마감일은 상세에서 온다
- robots.txt: 공용 fetch 클라이언트의 robots 확인을 통과했다
- 권장 주기: **승격 전에 아래 `detail.body` 를 손보정한다**

## 목록 API 는 없다

작업 지시에 적혀 있던 `api-public.toss.im/.../workspaces/13/posts` 는 공고 목록이 아니라
커리어 아티클 목록이다(제목이 "토스뱅크 Server Chapter와 Server Platform Team이 일하는 방식"
같은 글 제목이다). 2026-08-25 에 목록 페이지를 렌더하며 관찰한 JSON 응답은 푸터·배너·헤더
셋뿐이고 공고 배열을 담은 것이 없었다. 공고 263건은 초기 HTML 안에 `href="/career/job-detail?job_id=..."`
로 이미 들어 있다.

그래서 목록은 렌더로 남는다. 그 세 응답은 "후보가 없는 응답" 픽스처로 남겨 뒀다
(`tests/fixtures/toss-*-20260825.json`).

## 상세 페이지 모양이 공고마다 다르다

**이 사이트의 함정이다.** 일반 공고와 "집중 채용" 랜딩 페이지가 같은 `job-detail` 주소를
쓰는데 마크업이 다르다. 2026-08-25 측정에서 `job_id=5853067003` 에는 `h1` 이 있고
`job_id=7827417003` 에는 없다.

본문을 담은 요소를 구분하는 유일한 표시가 자동 생성 클래스다. 페이지 안에
`div.p-container__inner` 가 둘인데 앞의 것이 네비게이션(37자), 뒤의 것이 공고 본문이고,
둘을 가르는 것은 부모의 `css-6bhaou` 뿐이다.

| 저장된 값 | 결과 |
|---|---|
| `div.css-1d3w5wq` (크롤러 26) | 세 건 중 한 건만 본문을 읽었다. 나머지는 `parse` 실패 |
| `.p-container__inner` (크롤러 33) | 세 건 다 성공으로 끝났지만 본문이 네비게이션 37자다 |

두 번째가 더 위험하다. 실패로 보이지 않으면서 값이 틀린다.

손보정할 값은 이것이다. 2026-08-25 에 두 공고에서 확인했다.

```
.p-container.css-6bhaou .p-container__inner
```

자동 생성 클래스에 기대는 자리이므로 토스가 배포하면 여기가 먼저 깨진다. 깨지면 본문이
0건으로 떨어져 `parse` 실패로 드러난다.

## 마감일은 대부분의 공고에 없다

토스 상세에서 마감일을 말하는 자리는 `모집 기간` 을 이름표로 단 `blockquote` 하나뿐이다.
2026-08-26 에 수집한 264건 중 본문에 그 글자가 있는 것은 **4건**이고, 제목에 `~8/31` 처럼
날짜가 적힌 것은 2건이다. 나머지는 마감일을 밝히지 않는 상시 공고다.

`blockquote` 만 적어 두면 그 자리에 오는 것은 현직자 인터뷰 인용문이다. 264건 중 138건이
인용문을 마감일로 받았고, 값이 날짜가 아니라 `deadline` 의 `date_parse` 규칙이 실패했다.
그 실패는 칸 하나가 비는 것으로 끝나지 않는다 — 공고가 통째로 `normalized_jobs` 에서
빠진다 (`app/normalize/engine.py`). 264건 중 126건만 들어와 있던 것이 그 결과다.

**빈 마감일이 맞는 답이다.** 인용문을 대신 넣는 것은 소비 측이 그것을 마감일로 읽게 만든다.

이름표를 `strong` 으로 요구하는 이유는, 인용문 본문에 "모집 기간" 이라는 글자가 우연히
들어 있어도 잡히지 않게 하기 위해서다.

## 이력

| 날짜 | 일 | 원인 | 조치 |
|---|---|---|---|
| 2026-08-25 | 최초 등록(크롤러 26) | 본문 셀렉터가 `div.css-1d3w5wq` 라 3건 중 2건이 실패 | 해시 클래스를 고르지 말라고 프롬프트에 적었다 |
| 2026-08-25 | 재등록(크롤러 33) | 본문이 네비게이션 37자로 잡힌다 | 승격 전 `detail.body` 손보정이 필요하다 |
| 2026-08-26 | 마감일 264건 중 0건 | `blockquote` 가 현직자 인터뷰 인용문을 잡았다. 값이 날짜가 아니라 `date_parse` 가 실패했고 138건이 정규화에서 통째로 빠졌다 | 모집 기간을 이름표로 단 `blockquote` 만 잡도록 좁혔다. 픽스처 두 건으로 `tests/test_toss_deadline.py` 가 본다 |

## 칸 매핑 (2026-08-26, 본문 나누기 Push 2)

`crawlers` 33번 행의 설정에서 그대로 옮겼다. 같은 값이
`seeds/site-configs-20260826.json` 에 있고 `tests/test_split_body_mapping.py` 가 픽스처에
돌려 본다. 문서와 저장된 설정이 갈리지 않는지는 `tests/test_site_recipe_mapping.py` 가 본다.

목록은 렌더, 상세는 정적이다. 본문 셀렉터 .p-container__inner 가 상단 내비게이션을 먼저 잡고 있었다 —
:not(.p-navbar__inner-container) 를 붙였다. 상세 마크업이 공고마다 달라 제목·본문·마감일 말고는 비운다. 받은 픽스처가 집중채용 랜딩 한
건이라 그 한 건으로 넓혀 말하지 않는다.

| 칸 | 어디서 | 자리 |
|---|---|---|
| 제목 | 상세 HTML | `title` |
| 본문 원문 | 상세 HTML | `.p-container__inner:not(.p-navbar__inner-container)` |
| 필수 조건 | 비움 | 사이트가 이 값을 따로 주지 않는다 |
| 모집 마감일 | 상세 HTML | `blockquote:has(strong:-soup-contains("모집 기간"))` |
| 조직·부서 | 비움 | 사이트가 이 값을 따로 주지 않는다 |
| 모집 기업 | 비움 | 사이트가 이 값을 따로 주지 않는다 |
| 모집 시작일 | 비움 | 사이트가 이 값을 따로 주지 않는다 |
| 직군 | 비움 | 사이트가 이 값을 따로 주지 않는다 |
| 고용형태 | 비움 | 사이트가 이 값을 따로 주지 않는다 |
| 경력 구분 | 비움 | 사이트가 이 값을 따로 주지 않는다 |
| 근무지 | 비움 | 사이트가 이 값을 따로 주지 않는다 |
| 모집인원 | 비움 | 사이트가 이 값을 따로 주지 않는다 |
| 주요 업무 | 비움 | 사이트가 이 값을 따로 주지 않는다 |
| 우대 조건 | 비움 | 사이트가 이 값을 따로 주지 않는다 |
| 전형 절차 | 비움 | 사이트가 이 값을 따로 주지 않는다 |
| 기타 | 비움 | 사이트가 이 값을 따로 주지 않는다 |

빈 칸은 그 사이트가 그 값을 따로 주지 않는다는 사실이다. 다른 값으로 채우지 않는다 —
한화 `department` 에 근무지가 들어가 있던 것이 그렇게 생긴 버그다.
