# 수집 방식 네 가지 실측 (2026-08-24)

수집을 OCR 로 바꾸기 전에, 같은 상세 페이지에 네 방식을 돌려 시간과 토큰을 쟀다.
저장소 코드는 고치지 않았고 스크래치패드의 별도 스크립트로 측정했다.

## 무엇을 쟀나

| 방식 | 하는 일 |
|---|---|
| 1. 정적 파싱 | httpx 로 HTML 을 받아 셀렉터로 뽑는다 |
| 2. 렌더 파싱 | Playwright 로 렌더한 뒤 같은 셀렉터로 뽑는다 |
| 3. OCR 텍스트만 | 캡처해서 모델에 넘겨 글자만 받는다 |
| 4. OCR + 분류 | 캡처해서 글자를 받고, 13개 항목으로 나눈 결과도 받는다 |

모델은 `gemini-3.5-flash-lite`. 캡처는 1280px 폭, 전체 페이지, JPEG 품질 75.
셀렉터는 DB 에 저장된 것을 그대로 읽어 썼다.

대상은 두 곳이다. 정적으로 되는 곳과 안 되는 곳을 같이 봐야 비교가 성립한다.

- 롯데: `https://recruit.lotte.co.kr/apply/announcement/detail/21931885`
- LG: `https://careers.lg.com/apply/detail?id=1002099` (SPA)

## 결과

### 롯데 (정적으로 되는 곳)

| 방식 | 초 | 입력 토큰 | 출력 토큰 | 필드 수집 |
|---|---|---|---|---|
| 1. 정적 파싱 | 0.18 | 0 | 0 | 4/4 |
| 2. 렌더 파싱 | 5.17 | 0 | 0 | 4/4 |
| 3. OCR 텍스트만 | 8.84 | 1,137 | 920 | 글자 1,460자 |
| 4. OCR + 분류 | 10.62 | 2,294 | 1,330 | 4/4, 13개 항목 중 12개 채움 |

### LG (SPA)

| 방식 | 초 | 입력 토큰 | 출력 토큰 | 필드 수집 |
|---|---|---|---|---|
| 1. 정적 파싱 | 0.18 | 0 | 0 | **0/4** |
| 2. 렌더 파싱 | 29.19 | 0 | 0 | 4/4 |
| 3. OCR 텍스트만 | 32.94 | 1,110 | 839 | 글자 1,354자 |
| 4. OCR + 분류 | 34.89 | 2,240 | 1,316 | 4/4, **13개 항목 전부 채움** |

LG 의 29초는 측정 방식 탓이 크다. `networkidle` 을 25초까지 기다렸는데 이 페이지는 그 상태에
도달하지 않아 대기가 끝까지 찼다. 저장소의 렌더러는 항목이 나타나면 진행한다.

## 캡처 대기를 바꿔 다시 잰 값 (LG)

| 대기 방식 | 초 | 이미지 | 읽힌 글자 | 입력 토큰 |
|---|---|---|---|---|
| 내용 셀렉터만 기다림 | 2.13 | 145KB | 1,353자 | 1,095 |
| 셀렉터 + 1.5초 | 3.15 | 145KB | 1,363자 | 1,095 |
| 셀렉터 + 3초 | 3.54 | 145KB | 1,372자 | 1,095 |

**캡처는 3초대에 끝난다.** 29초가 아니다. 다만 셀렉터만 기다린 회차에서 문서 높이가 아직
900px(뷰포트 그대로)로 잡히는 것이 관측됐고, 별도 회차에서는 21KB짜리 잘린 캡처가 나왔다.
**대기가 부족하면 조용히 잘린다.** 셀렉터 확인 뒤 1.5초 정도를 더 두는 편이 안전하다.

## 1~4번에서 읽은 것

**시간은 캡처가 지배한다.** 모델 호출은 텍스트 추출 3.4~4.2초, 분류 2.5초다. 캡처 대기를
어떻게 잡느냐가 전체 시간을 정한다.

**토큰은 크지 않다.** 공고 하나에 입력 2,240~2,294, 출력 1,316~1,330. 이미지가 길어지면 입력이
타일 수에 비례해 는다. 단가는 바뀌므로 현재 Gemini 문서에서 확인해야 한다
(`.claude/rules/llm.md`).

**분류가 OCR 을 쓰는 이유다.** 셀렉터는 등록된 필드 4개만 준다. 같은 화면에서 분류는 13개
항목을 채웠다 — LG 는 13개 전부, 롯데는 12개. 셀렉터를 13개로 늘리려면 사이트마다 13번 맞춰야
하는데, 분류는 화면을 읽고 있는 것만 채운다.

**정적으로 되는 곳에서 OCR 은 59배 느리다.** 롯데 0.18초 대 10.62초. 정적으로 4/4 가 나오는
페이지에 OCR 을 쓰는 것은 손해다.

**LG 는 API 가 더 낫다.** API 는 제목·회사·마감·본문·자격요건을 구조화해서 주고 토큰을 쓰지
않는다. 캡처해서 글자로 되돌리면 정확도는 떨어지고 호출이 둘 는다.

## 5·6번: HTML 을 통째로 모델에 주기 (캡처도 OCR 도 없이)

캡처를 거치지 않고 페이지 내용을 바로 모델에 넘겨 13개 항목을 받는 경로다.

| 방식 | 하는 일 |
|---|---|
| 5. 렌더 HTML(속성정리) | 렌더한 HTML 에서 script·style·svg 를 걷고 속성을 class·id·href 만 남겨 그대로 넘긴다 |
| 6. 렌더 텍스트만 | 렌더한 HTML 에서 텍스트만 뽑아 넘긴다 |

### 결과

| 사이트 | 방식 | 초 | 입력 토큰 | 출력 토큰 | 채운 항목 | 보낸 크기 |
|---|---|---|---|---|---|---|
| 롯데 | 5. HTML(속성정리) | 5.42 | 18,758 | 449 | 13/13 | 46,165B |
| 롯데 | 6. 텍스트만 | **4.87** | **4,416** | **391** | 13/13 | 6,945B |
| LG | 5. HTML(속성정리) | 5.40 | 55,377 | 433 | 13/13 | 153,242B |
| LG | 6. 텍스트만 | **5.13** | **4,034** | **419** | 13/13 | 5,818B |

### 정제 단계별 크기

토큰 상한(1,048,576)을 넘겨 한 번 실패했다. 무엇이 부피를 차지하는지 재보면 이렇다.

| 사이트 | 원본 | script·style 제거 | 속성 정리 | 텍스트만 |
|---|---|---|---|---|
| 롯데 (정적) | 148,440B | 56,300B | 43,606B | 6,622B |
| 롯데 (렌더) | 149,488B | 61,292B | 46,156B | 6,945B |
| LG (정적) | 1,867B | 665B | 286B | 0B |
| LG (렌더) | **1,591,570B** | 1,519,133B | 153,242B | 5,818B |

LG 렌더 HTML 은 script·style 을 걷어내도 1.5MB 다. emotion 이 만든 클래스 해시와 인라인
속성이 대부분이고, 속성을 정리하면 10분의 1로 준다. **텍스트만 뽑으면 다시 26분의 1이 된다.**

### 값이 맞는지 대조

채운 개수만 보면 안 된다. 같은 공고(LG `id=1002099`)를 OCR 경로와 텍스트 경로로 뽑아 견줬다.

| 항목 | OCR + 분류 | 렌더 텍스트만 | |
|---|---|---|---|
| title | `[NW부문] 보건관리자 계약직 채용` | 같음 | 일치 |
| parent_company | `LG` | 같음 | 일치 |
| hiring_company | `LG유플러스` | 같음 | 일치 |
| employment_type | `계약직` | 같음 | 일치 |
| location | `서울특별시 강서구, 경기도 수원시, 부산광역시 동구` | 같음 | 일치 |
| start_date | `2026.08.21 09:00` | `2026.08.21` | 텍스트 쪽이 시각을 뺐다 |
| end_date | `2026.08.30 23:00` | `2026.08.30` | 같음 |

본문에서 **OCR 이 글자를 두 군데 틀렸다.**

| | OCR 이 읽은 것 | 원본 (`raw_jobs` 확인) |
|---|---|---|
| role_summary | `유해요**일** 관리` | `유해요**인** 관리` |
| must_have | `산업안전보건법 보건관리자` | `산업안전보건법**상** 보건관리자` |

원본에 `유해요인` 1번, `유해요일` 0번, `산업안전보건법상` 1번이 있다. 텍스트 경로는 원본과
글자까지 일치했다. **DOM 에서 뽑은 텍스트는 이런 오독을 할 수 없다.**

시각이 빠진 것은 프롬프트와 스키마 문제이지 경로의 한계가 아니다.

## 여덟 가지를 한 표로

같은 공고 하나를 처리하는 비용이다. LG 기준이고, 캡처는 뒤에서 다시 잰 3.5초를 쓴다.

| 방식 | 초 | 토큰(입력+출력) | 이미지 저장 | 얻는 항목 |
|---|---|---|---|---|
| 1. 정적 파싱 | 0.18 | 0 | 없음 | 0/4 (SPA 라 실패) |
| 2. 렌더 파싱 | 3.5 | 0 | 없음 | 4/4 (등록된 셀렉터만) |
| 3. OCR 텍스트만 | 7.0 | 1,949 | 145KB | 글자만 |
| 4. OCR + 분류 | 9.5 | 3,556 | 145KB | 13/13, **글자 오독 2건** |
| 5. 렌더 HTML(속성정리) | 5.4 | 55,810 | 없음 | 13/13 |
| 6. 렌더 텍스트만 | **5.1** | **4,453** | **없음** | **13/13, 원본과 일치** |

## 결론

**6번이 이긴다.** 시간은 OCR 경로의 절반, 토큰은 5번의 8분의 1, 이미지 저장이 없고, 채운 항목은
같은데 글자는 원본과 일치한다.

OCR 은 화면을 사진으로 만들었다가 다시 글자로 되돌리는 왕복이다. DOM 에 이미 글자가 있는 페이지
에서는 그 왕복이 시간·저장·정확도 셋 다 손해다.

**OCR 이 필요한 경우는 남는다** — 글자가 이미지로만 있는 공고(포스터 한 장으로 올라온 채용공고),
DOM 에서 텍스트를 뽑을 수 없는 캔버스 렌더링. 그때는 4번이 유일한 길이다. 기본이 아니라 예외로
두는 편이 측정에 맞는다.

**분류는 어느 경로에서든 값어치가 있다.** 셀렉터는 등록된 4개만 주는데 분류는 13개를 채웠다.
본문을 확보하는 방법(정적·API·렌더·캡처)과 분류하는 단계를 나누면, 사이트마다 제일 싼 길로
본문을 얻고 분류는 공통으로 쓸 수 있다.

## 저장 용량

전체 페이지 JPEG 는 롯데 279KB, LG 145KB 였다. 앞서 잰 다른 LG 공고는 187KB 다.
공고당 150~280KB 로 보면 된다.

지금 216건이면 32~60MB. **이미지는 SQLite 밖에 둬야 한다.** DB 가 1.9MB 인데 이미지를 넣으면
스냅샷 내보내기·가져오기가 못 쓰게 된다. 파일로 두고 DB 에는 경로만 남긴다.

## 다시 재려면

`bench.py`, `bench3.py`, `sizes.py`, `shot3.py` 가 스크래치패드에 있다. 저장소에 넣지 않은 것은 실사이트를 때리는
스크립트이기 때문이다. 다시 잴 일이 생기면 이 문서의 수치와 견주면 된다.
